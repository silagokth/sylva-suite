// Protobuf file for memory images
// to be used for input, output and global memories

syntax = "proto3";

message image_line {
  fixed64 address = 1;
  int64   value   = 2;
}

message mem_image {
  repeated image_line line = 1;
}
